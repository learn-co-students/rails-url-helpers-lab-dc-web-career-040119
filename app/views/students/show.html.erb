
<h1><%= @student.to_s %></h1>
<% if @student.active == false
      active_status = "inactive"
    else
      active_status = "active"
    end
        %>
<div><%= "This student is currently #{active_status}." %></div>

<a href="/students">I am a link</a>
<%=  link_to("hi", students_path)  %>
